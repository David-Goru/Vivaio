using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VideoCheats : MonoBehaviour
{
    /*bool normalTime = true;

    void Update()
    {
        if (Input.GetKeyDown("b"))
        {
            if (normalTime) (GameObject.Find("Farm handler").GetComponent("TimeSystem") as TimeSystem).MinuteValue = 0.0035f;
            else (GameObject.Find("Farm handler").GetComponent("TimeSystem") as TimeSystem).MinuteValue = 0.35f;

            normalTime = !normalTime;
        }
        
        if (Input.GetKeyDown("t"))
        {
            // Infinite carrot seeds
            SeedTool seedTool = (GameObject.Find("Tools").transform.Find("Seed").GetComponent("SeedTool") as SeedTool);
            seedTool.SeedName = "Carrot";
            seedTool.Remaining = 1000;
            seedTool.TakeTool();
        }
        if (Input.GetKeyDown("x"))
        {
            // Throw seeds
            if (PlayerTools.ToolOnHand.name == "Seed") PlayerTools.ToolOnHand.LetTool();
            GameObject.Find("UI").transform.Find("Throw seeds").gameObject.SetActive(false);
        }
        if (Input.GetKeyDown("y"))
        {
            // Infinite potato seeds
            SeedTool seedTool = (GameObject.Find("Tools").transform.Find("Seed").GetComponent("SeedTool") as SeedTool);
            seedTool.SeedName = "Potato";
            seedTool.Remaining = 1000;
            seedTool.TakeTool();
        }
        if (Input.GetKeyDown("u"))
        {
            // Infinite tomato seeds
            SeedTool seedTool = (GameObject.Find("Tools").transform.Find("Seed").GetComponent("SeedTool") as SeedTool);
            seedTool.SeedName = "Tomato";
            seedTool.Remaining = 1000;
            seedTool.TakeTool();
        }
        if (Input.GetKeyDown("i"))
        {
            // Infinite pumpkin seeds
            SeedTool seedTool = (GameObject.Find("Tools").transform.Find("Seed").GetComponent("SeedTool") as SeedTool);
            seedTool.SeedName = "Pumpkin";
            seedTool.Remaining = 1000;
            seedTool.TakeTool();
        }
    }*/
}